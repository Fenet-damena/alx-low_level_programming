#include "main.h"
/**
 * _strlen - the function that return the length of string
 * @str: string to print
 * Return the length of stsring
 */
int _strlen(char *s)
{
	while (*str != '\0')
	{
		_putchar(*str++);
	}
	_putchar('\n');
}
